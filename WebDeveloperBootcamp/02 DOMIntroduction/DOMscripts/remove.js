// removeChild - we need to select a parent of an element we want to remove

const firstLi = document.querySelector("li");
firstLi.removeChild;
const ul = document.querySelector("ul");
ul.removeChild(firstLi);

// remove - easier, we use it on an element we want to remove
const img = document.querySelector("img");
img.remove();
